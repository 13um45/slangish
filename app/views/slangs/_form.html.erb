<%= form_for @slang do |f| %>
 
  <% if @slang.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@slang.errors.count, "error") %> prohibited
        this slang from being saved:
      </h2>
      <ul>
        <% @slang.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :word %><br>
    <%= f.text_field :word, class: "form-control" %>
  </p>
 
  <p>
    <%= f.label :definition %><br>
    <%= f.text_area :definition, class: "form-control", rows: 5 %>
  </p>
 
  <p>
    <%= f.submit "Submit", class: "btn btn-default" %>
  </p>
 
 <% end %>