<%= form_for @slang do |f| %>
 
  <% if @slang.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@slang.errors.count, "error") %> prohibited
        this slang from being saved:
      </h2>
      <ul>
        <% @slang.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 